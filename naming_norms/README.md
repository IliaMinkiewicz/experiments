# Апробация называний — макет

Сбор норм называния для 94 новых стимулов + 12 якорей из существующей базы.
Показывается только серая (однозначная) версия. Задача участника — написать название
и оценить лёгкость называния по шкале 1–7.

**Состояние: не опубликовано.** В `config.js` стоит `enabled: false`, страница
показывает «набор закрыт». Данные не отправляются.

---

## Состав

| Файл / папка | Что это |
|---|---|
| `index.html`, `app.js`, `style.css` | само задание, без внешних библиотек |
| `config.js` | всё, что правится руками перед запуском |
| `stimuli.json` | публичная часть: id, файл, номер списка |
| `ключ_стимулов.csv` | **не выкладывать**: рабочие названия, якоря, старые нормы |
| `img/` | 106 изображений (94 добора + 12 якорей) |
| `train/` | 3 тренировочные картинки (лестница, конверт, метла) |
| `google_apps_script.js` | приёмник данных |

Три списка: 45 / 45 / 40 карточек, в каждом 12 якорей. Все 23 пары
«развести по блокам» разведены по разным спискам. Seed раскладки — 20260812.

## Архитектура

Всё живёт в репозитории `IliaMinkiewicz/experiments`, но в **двух разных ветках**:

| Что | Где |
|---|---|
| задание (эта папка) | ветка `main`, каталог `naming_norms/` — отсюда собирается Pages |
| данные участников | ветка `data`, каталог `naming_norms/data/` |

Разделение по веткам обязательно. Pages пересобирается на каждый коммит в свою ветку,
лимит — около 10 сборок в час. При 65 участниках на список набегает под тысячу коммитов,
и страница задания начнёт отдавать ошибку в разгар набора. В отдельной ветке коммиты
данных пересборку не запускают.

## Развёртывание

**1. Ветка для данных.** Один раз, локально:

```bash
git clone https://github.com/IliaMinkiewicz/experiments.git
cd experiments
git checkout --orphan data      # ветка без истории main
git rm -rf .
mkdir -p naming_norms/data
touch naming_norms/data/.gitkeep
git add naming_norms/data/.gitkeep
git commit -m "ветка data: приём данных апробации называний"
git push origin data
git checkout main
```

**2. Токен.** Settings → Developer settings → Personal access tokens → Fine-grained.
Доступ только к `experiments`, права **Contents: Read and write**, срок 90 дней.
Нигде не коммитить — он живёт только внутри Apps Script.

**3. Apps Script.** Новый проект на script.google.com, вставить `google_apps_script.js`,
заполнить блок НАСТРОЙКИ (`BRANCH = "data"` уже проставлена). Развернуть как веб-приложение:
запуск от своего имени, доступ «у всех». Запустить `selfTest()` — в ветке `data` должен
появиться `naming_norms/data/_selftest.json`. **Проверить, что в `main` ничего не появилось.**
Удалить тестовый файл.

**4. Задание.** Содержимое этой папки положить в `main` в каталог `naming_norms/`.
Pages на репозитории уже включён (там работает RMET), отдельно настраивать не нужно.

**5. `config.js`:** вписать `uploadUrl` (URL из шага 3) и `secret` (то же значение, что в Apps Script).

**6. Проверить перед запуском** — см. чек-лист ниже. Затем `enabled: true`.

## Ссылки для трёх списков

```
https://iliaminkiewicz.github.io/experiments/naming_norms/?list=1&w=ИДЕНТИФИКАТОР_ИСПОЛНИТЕЛЯ
https://iliaminkiewicz.github.io/experiments/naming_norms/?list=2&w=ИДЕНТИФИКАТОР_ИСПОЛНИТЕЛЯ
https://iliaminkiewicz.github.io/experiments/naming_norms/?list=3&w=ИДЕНТИФИКАТОР_ИСПОЛНИТЕЛЯ
```

`w` — идентификатор исполнителя, подставляемый площадкой. Если подстановка не настроена,
параметр можно опустить: участнику присвоится случайный `anon_*`, а связь с выплатой
пойдёт через код завершения.

## Яндекс Задания

1. **Три отдельных проекта**, по одному на список — так видно набор по каждому и
   управляются квоты.
2. Тип: задание с внешней ссылкой и полем ввода кода завершения.
3. **Запрет пересечения.** Исполнитель, прошедший один список, не должен видеть остальные.
   Механика — через навык: по завершении проекта 1 исполнителю выдаётся навык
   `naming_done`, а в проектах 2 и 3 стоит фильтр «навык `naming_done` не выдан».
   Без этого один человек пройдёт все три списка, и ответы по якорям станут зависимыми.
4. Фильтры: регион РФ, возраст 18+, русский язык. Жёстких требований не ставим —
   нужна разнородная выборка носителей языка.
5. Приёмка **не автоматическая**: код из формы сверяется с полем `code` в данных.
6. Цена — по медианному времени с пилота, не заранее.
7. **Пилот 10 человек на списке 1** до основного набора.

Целевой набор: 65 человек на список (ожидаемые ~50 после отбраковки).
Якоря наберут около 150 ответов, новые стимулы — около 50.

## Что проверить до публикации

- [ ] `selfTest()` записал файл в ветку `data`, а в `main` ничего не появилось
- [ ] после нескольких тестовых прогонов Pages не пересобирался (Actions → нет новых сборок)
- [ ] пройти все три списка целиком, данные легли в `naming_norms/data/L1..L3` ветки `data`
- [ ] открыть исходник страницы и вкладку Network: рабочих названий стимулов там быть не должно
- [ ] проверить из РФ без VPN: домашний интернет и мобильный двух операторов
- [ ] Chrome, Firefox, Яндекс.Браузер
- [ ] закрыть вкладку на середине → данные до последней порции уже в репозитории
- [ ] отключить сеть на 20-й карточке → в конце появляется экран ручной отправки
- [ ] код завершения совпадает с полем `code` в файлах
- [ ] `ключ_стимулов.csv` не попал в публичный репозиторий

## Формат данных

Одна порция = один файл `data/L{список}/{pid}__{код}__{время}.json`.
Внутри `rows` — по записи на карточку:

`code, pid, worker, list, seed, version, kind, pos, stim_id, file, check,
answer, latin, ease, rt_first_key, rt_submit, ts`

`kind`: `main` — обычная карточка, `check` — прямая проверка внимания.
`check: "attention"` — стимул-якорь со старым согласием 100 %.
`latin` — ответ набран латиницей (не исправляется автоматически, решается на анализе).
Файл с `__final` содержит ещё и `meta`: демография, время, разрешение экрана, user-agent.

## Известные ограничения

**Кнопки «не знаю» нет** — по решению исследователя. Незнакомый объект участник
всё равно как-то назовёт, поэтому «не узнал» в данных неотличим от «узнал неверно».
Частичный индикатор — оценка лёгкости 6–7.

**Точная формулировка шкалы** из опроса 2022 года не сохранилась; используется
реконструкция «Насколько легко было подобрать название? 1 — очень легко, 7 — очень сложно»
с тем же направлением (1 = легко). При сравнении с якорями это надо оговаривать.

**`secret` виден в исходнике страницы.** Он отсекает случайный мусор, но не защищает
от намеренной подделки. Ограничения на стороне Apps Script: длина запроса и флаг `ACCEPTING`.
