// ============================================================
//  НАСТРОЙКИ АПРОБАЦИИ НАЗЫВАНИЙ
//  Правится вручную перед запуском. Кода трогать не нужно.
// ============================================================
const CONFIG = {
  // false — страница показывает «набор закрыт» и данные не отправляются.
  // Переключить в true в момент публикации.
  enabled: false,

  // URL веб-приложения Google Apps Script (Развернуть -> Веб-приложение -> Все).
  uploadUrl: "https://script.google.com/macros/s/AKfycbx5G1owePM3LgE5KlciV0ROCa2uiihCSdUnilLTJ4OQqi4Jj3n_peOcxvSnTAo-UPLc/exec",

  // Должен совпадать со значением в google_apps_script.js
  secret: "NAMING_NORMS_2026",

  // Версия сборки — пишется в каждую запись, помогает отличить пилот от основного сбора.
  version: "naming-1.0",

  // Отправлять накопленное каждые N карточек (страховка от потери при обрыве).
  chunkSize: 10,

  // Минимальная ширина окна: задание только с компьютера.
  minWidth: 900,

  // Показывать ли отладочную панель (только для тестирования).
  debug: false
};
